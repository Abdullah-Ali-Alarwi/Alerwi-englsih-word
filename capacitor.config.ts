import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alarwi.vocab',      // ضع أي Package ID يناسبك
  appName: 'alrwiy-words',        // اسم التطبيق
  webDir: 'out'                  // مجلد البناء بعد npm run build
};

export default config;
